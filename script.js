//variable to keep track of where in the light sequence the page is (current state)
var sequenceNum = 0; 

//function called when button is pressed
function lightSequence(){
  
  //You will need an IF statement to check the current state and update the HTML elements accordingly using the pic variable.  
 
}